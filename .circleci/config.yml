# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    working_directory: ~/project
    docker:
    # Primary container image where all steps run.
     - image: circleci/node:10.9-stretch-browsers
    steps:
      # command will execute in trusty container
      # and can access mongo on localhost
      - checkout
      - setup_remote_docker
#  - run cd ~/project
      - run: ls -lah
      - run: yarn install
      - run: docker-compose up --build -d database
      - run: docker-compose up --build server-test
# - run: make setup-test-database
# - run: make run-test-migration
# - run: make run-seeds
# - run: make test
